@model IEnumerable<BDProject.Models.Inventory>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.quantity_on_hand)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ProductInformation.product_name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Warehouse.warehouse_spec)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.quantity_on_hand)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ProductInformation.product_name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Warehouse.warehouse_spec)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
        </td>
    </tr>
}

</table>
